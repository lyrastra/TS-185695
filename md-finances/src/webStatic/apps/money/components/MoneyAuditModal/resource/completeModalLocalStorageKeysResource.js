import { getId } from '@moedelo/frontend-core-v2/helpers/companyId';

export default {
    auditComplete: `MoneyModalAuditCompleteDate__${getId()}`,
    reconciliationComplete: `MoneyModalReconciliationCompleteDate__${getId()}`
};
